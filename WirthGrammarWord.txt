INT a=0;
INT b=a+8;

DECLARE INT f (INT c, INT d, INT e);
DECLARE VOID h ();

READ b;
INT g=9;

WHILE g > 0 {
	IF a < b THEN {
		READ g;
		CALL f (a, g, g);
		LET a=b*(3)
	} ELSE {
		CALL h(void);
		PRINT g;
		LET a=b/(3)
	}
};

CALL f(a, b, g);

FUNCTION VOID h () {
	LET a=a*(a+b-2)
};

FUNCTION INT f (INT c, INT d, INT e) {
	LET c=c+d+e;
	PRINT c

	GIVE c
}

END
